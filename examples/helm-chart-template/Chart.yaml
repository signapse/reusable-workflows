apiVersion: v2
name: my-app
description: A Helm chart for Kubernetes application deployment
type: application

# Chart version (update when you make changes to the chart)
version: 1.0.0

# Application version (should match your Docker image tag)
appVersion: "1.0.0"

keywords:
  - application
  - deployment

maintainers:
  - name: Your Team
    email: team@example.com
